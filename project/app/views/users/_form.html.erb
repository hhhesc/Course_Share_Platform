<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :username, "用户名", style: "display: block" %>
    <%= form.text_field :username, class: "form-control" %>
  </div>

  <div>
    <%= form.label :password, "密码", style: "display: block" %>
    <%= form.text_field :password, class: "form-control" %>
  </div>

  <div>
    <%= form.label :email, "邮箱", style: "display: block" %>
    <%= form.text_field :email, class: "form-control" %>
  </div>

  <div>
    <p></p>
    <p><%= form.submit "提交", class: "btn btn-primary" %></p>
  </div>
<% end %>

<%= link_to "返回用户界面", user_path(@user), class: "btn btn-secondary" %>
